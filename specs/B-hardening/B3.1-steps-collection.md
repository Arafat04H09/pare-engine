# SPEC: B3.1 — Pipeline Steps: Crawl & Query

## Priority
B — Parallel

## Dependencies
- Blocks: []
- Blocked by: [A6]

## Files OWNED (may create or modify)
- apps/audit-runner/src/steps/crawl.ts
- apps/audit-runner/src/steps/query-engines.ts

## Files READ-ONLY (may import from, never modify)
- packages/core/src/tools/crawl-site.js
- packages/core/src/tools/query-engines.js

## Acceptance Criteria
1. [ ] `crawl.ts`: Wrap `executeCrawlStep` in `step.run()`, handle Firecrawl pagination.
2. [ ] `query-engines.ts`: Wrap `executeQueryStep` in `step.run()`, handle multi-platform queries.
3. [ ] Implement proper retry policies for transient API failures.
4. [ ] Ensure outputs strictly follow `@pare-engine/core/contracts`.

## Implementation Notes
- Rule #10: Failures caught and logged, not thrown.
- Return `CrawlOutput` and `MultiProviderResult`.

## Verification Command
pnpm test apps/audit-runner/src/steps/crawl.test.ts
