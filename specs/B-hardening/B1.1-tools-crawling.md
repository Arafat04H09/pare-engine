# SPEC: B1.1 — Core Tools: Crawling & Extraction

## Priority
B — Parallel

## Dependencies
- Blocks: []
- Blocked by: [A6]

## Files OWNED (may create or modify)
- packages/core/src/tools/crawl-site.ts
- packages/core/src/tools/extract-media.ts
- packages/core/src/tools/parse-jsonld.ts
- packages/core/src/tools/bot-analyzer.ts

## Files READ-ONLY (may import from, never modify)
- packages/core/src/contracts/crawl.contract.ts
- packages/core/src/config.ts

## Acceptance Criteria
1. [ ] `crawl-site`: Implement robust Firecrawl integration with timeout (30s) and retry logic.
2. [ ] `extract-media`: Identify all images, videos, and social links from markdown/HTML.
3. [ ] `parse-jsonld`: Extract structured data (Organization, LocalBusiness, FAQ) using Zod schemas.
4. [ ] `bot-analyzer`: Analyze robots.txt for AI-specific agents (GPTBot, Perplexity-bot).
5. [ ] All tools handle API failures gracefully (return empty/null instead of throwing).

## Implementation Notes
- Use `loadConfig()` for API keys.
- Follow Rule #12: Use Zod schemas + `generateObject()` for any LLM-based extraction.
- Catch external API failures and log them, but don't throw (Rule #10).

## Verification Command
pnpm test packages/core/src/tools/crawl-site.test.ts
