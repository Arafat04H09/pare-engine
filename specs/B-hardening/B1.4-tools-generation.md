# SPEC: B1.4 — Core Tools: Content Generation

## Priority
B — Parallel

## Dependencies
- Blocks: []
- Blocked by: [A6]

## Files OWNED (may create or modify)
- packages/core/src/tools/generate-faq.ts
- packages/core/src/tools/generate-jsonld.ts
- packages/core/src/tools/generate-llmstxt.ts
- packages/core/src/tools/content-optimizer.ts

## Files READ-ONLY (may import from, never modify)
- packages/core/src/contracts/proposal.contract.ts

## Acceptance Criteria
1. [ ] `generate-faq`: Create 5-10 SEO-optimized FAQs based on site content.
2. [ ] `generate-jsonld`: Generate valid Schema.org JSON-LD for LocalBusiness/Product.
3. [ ] `generate-llmstxt`: Create standard `llms.txt` and `llms-full.txt` markdown files.
4. [ ] `content-optimizer`: Suggest rewrites for pages with low answer-first scores.
5. [ ] All generation tools output clean markdown or valid JSON.

## Implementation Notes
- Use `generateText()` or `generateObject()` where appropriate.
- Ensure outputs are "copy-paste ready" for clients.
- Handle edge cases where site content is very thin.

## Verification Command
pnpm test packages/core/src/tools/generate-faq.test.ts
