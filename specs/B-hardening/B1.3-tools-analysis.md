# SPEC: B1.3 — Core Tools: LLM Analysis

## Priority
B — Parallel

## Dependencies
- Blocks: []
- Blocked by: [A6]

## Files OWNED (may create or modify)
- packages/core/src/tools/analyze-competitor.ts
- packages/core/src/tools/analyze-review-semantics.ts
- packages/core/src/tools/scan-hallucinations.ts
- packages/core/src/tools/aio-detect.ts

## Files READ-ONLY (may import from, never modify)
- packages/core/src/contracts/analysis.contract.ts

## Acceptance Criteria
1. [ ] `analyze-competitor`: Identify 3-5 key competitors from engine results.
2. [ ] `analyze-review-semantics`: Use LLM (Claude Haiku) to extract sentiment and themes.
3. [ ] `scan-hallucinations`: Verify engine claims against crawled site content.
4. [ ] `aio-detect`: Detect if content is AI-generated (basic patterns + heuristics).
5. [ ] All analysis tools use `generateObject()` from Vercel AI SDK.

## Implementation Notes
- Follow Rule #8: Sentiment analysis must be LLM-based, never keyword matching.
- Use `system` prompts from `packages/core/src/prompt-library`.
- Enforce strict JSON output via Zod.

## Verification Command
pnpm test packages/core/src/tools/analyze-review-semantics.test.ts
